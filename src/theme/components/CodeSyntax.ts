import { styled, Typography } from '@mui/material';

export const Variable = styled(Typography)(() => ({
  display: 'inline',
  color: '#b87ba3',
}));

export const VariableName = styled(Typography)(() => ({
  display: 'inline',
  color: '#8b78ce',
}));

export const Symbols = styled(Typography)(() => ({
  display: 'inline',
  color: '#108ac2',
}));
